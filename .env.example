# Fretomaton Environment Configuration
# Copy this file to .env and update with your actual values

# =============================================================================
# DEPLOYMENT CONFIGURATION
# =============================================================================

# SSH connection details for your web server
DEPLOY_REMOTE_USER=your_username
DEPLOY_REMOTE_HOST=your.server.ip.address

# Remote path where files will be deployed
# Example: /home/user/public_html/fretomaton
DEPLOY_REMOTE_PATH=/path/to/your/web/directory

# Local build directory (usually ./dist/)
DEPLOY_LOCAL_DIST_PATH=./dist/

# =============================================================================
# BUILD CONFIGURATION
# =============================================================================

# Base path for asset URLs in production builds
# This is critical for subfolder deployments to ensure assets load correctly
#
# Examples:
#   Root deployment:     VITE_BASE_PATH=/
#   Subfolder deployment: VITE_BASE_PATH=/fretomaton
#   Nested subfolder:    VITE_BASE_PATH=/tools/fretomaton
#
# The value should match the URL path where your app will be accessible:
# - If accessible at https://yourdomain.com/ use VITE_BASE_PATH=/
# - If accessible at https://yourdomain.com/fretomaton/ use VITE_BASE_PATH=/fretomaton
VITE_BASE_PATH=/fretomaton
